# Framework base
Django>=5.1,<6.0
djangorestframework==3.15.2
djangorestframework-simplejwt==5.3.1

# CORS
django-cors-headers==4.6.0

# Variables de entorno
django-environ==0.11.2

# Base de datos (psycopg3 — compatible con Python 3.14)
psycopg[binary]>=3.1

# Tareas asincronas
celery==5.4.0
redis==5.2.1

# Generacion de PDF
WeasyPrint==62.3
pydyf==0.11.0  # pin: pydyf>=0.12 rompe WeasyPrint 62.3 (AttributeError en pdf/stream.py)

# Generacion de DOCX (borrador editable para revision)
python-docx==1.1.2

# Excel
openpyxl==3.1.5

# Imagenes
Pillow>=10.0

# Graficas del informe diagnostico (DOCX)
matplotlib==3.10.8

# Servidor de produccion
gunicorn==23.0.0
whitenoise==6.8.2
